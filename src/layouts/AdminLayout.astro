---
import BaseLayout from './BaseLayout.astro';

const {
  title = 'Dev Notes CMS',
  active = '',
  pageClass = '',
  layoutClass = '',
  dataAttr = '',
} = Astro.props;

const pageAttrs = dataAttr ? { [dataAttr]: true } : {};

const navGroups = [
  {
    label: '内容',
    items: [
      { key: 'posts', label: '笔记', href: '/admin/posts' },
      { key: 'topics', label: '主线', href: '/admin/topics' },
      { key: 'watch', label: '影像档案', href: '/admin/watch' },
      { key: 'reading', label: '阅读书架', href: '/admin/reading' },
    ],
  },
  {
    label: '站点',
    items: [
      { key: 'home', label: '首页模块', href: '/admin/home' },
      { key: 'radar', label: '统计标签', href: '/admin/radar' },
      { key: 'map', label: '地图校准', href: '/admin/map' },
      { key: 'assistant', label: 'AI助手', href: '/admin/assistant' },
      { key: 'about', label: '关于', href: '/admin/about' },
      { key: 'site', label: '站点信息', href: '/admin/site' },
    ],
  },
];
---

<BaseLayout title={title} chrome={false} shell={false} assistant={false}>
  <main class:list={['cms-page', pageClass]} {...pageAttrs}>
    <header class="cms-topbar">
      <a class="cms-brand" href="/admin/site"><span>DN</span> Dev Notes CMS</a>
      <slot name="search">
        <input class="cms-search" type="search" value="Control Center" readonly />
      </slot>
      <div class="cms-actions">
        <slot name="actions">
          <a href="/" target="_blank" rel="noreferrer">预览前台</a>
        </slot>
        <button
          class="cms-theme-toggle"
          type="button"
          data-admin-theme-toggle
          aria-label="切换管理端主题"
          title="切换管理端主题"
        >
          <svg class="cms-theme-icon cms-theme-icon-sun" viewBox="0 0 24 24" aria-hidden="true">
            <circle cx="12" cy="12" r="4"></circle>
            <path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path>
          </svg>
          <svg class="cms-theme-icon cms-theme-icon-moon" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79Z"></path>
          </svg>
        </button>
        <form method="post" action="/api/admin/logout">
          <button type="submit">退出</button>
        </form>
      </div>
    </header>

    <div class:list={['cms-layout', layoutClass]}>
      <aside class="cms-sidebar">
        {navGroups.map((group) => (
          <>
            <span>{group.label}</span>
            {group.items.map((item) => (
              <a class={active === item.key ? 'active' : ''} href={item.href}>{item.label}</a>
            ))}
          </>
        ))}

      </aside>

      <section class="cms-main">
        <slot />
      </section>

      <slot name="editor" />
    </div>
  </main>
</BaseLayout>
